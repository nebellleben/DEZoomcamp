# Master File

For Data Engineering Zoomcamp Homework 1.

Build the database by running docker compose (refer to `docker-compose.yaml`):
```
docker compose pull
docker compose up -d
```

Refer to this markdown for details of the SQL queries applied: [here](queries.md)